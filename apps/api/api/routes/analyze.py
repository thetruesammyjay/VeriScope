"""Route contract for article classification plus current-source analysis.

The route will expose ``POST /api/v1/analyze``. It is intentionally not
registered by the placeholder ``main.py`` until the service dependencies and
provider configuration are implemented.
"""

