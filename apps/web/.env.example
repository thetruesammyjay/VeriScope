# Set this to the deployed Render API URL in Vercel.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Keep these aligned with MIN_ARTICLE_LENGTH and MAX_ARTICLE_LENGTH on the API.
NEXT_PUBLIC_MIN_ARTICLE_LENGTH=100
NEXT_PUBLIC_MAX_ARTICLE_LENGTH=20000
